services:
  streamantix:
    build: .
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./.secrets:/app/.secrets
    ports:
      - "${OVERLAY_PORT:-8080}:${OVERLAY_PORT:-8080}"
      - "${OAUTH_CALLBACK_PORT:-4343}:4343"
    restart: unless-stopped
